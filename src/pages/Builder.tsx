import React from 'react';
import { Typography, Box } from '@mui/material';

const Builder: React.FC = () => {
  return (
    <Box>
      <Typography variant="h4" gutterBottom>
        Builder Page
      </Typography>
      <Typography>
        This is the Builder page. Content will be added later.
      </Typography>
    </Box>
  );
};

export default Builder;