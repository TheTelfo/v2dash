import React from 'react';
import { Typography, Box } from '@mui/material';

const Reports: React.FC = () => {
  return (
    <Box>
      <Typography variant="h4" gutterBottom>
        Reports Page
      </Typography>
      <Typography>
        This is the Reports page. Content will be added later.
      </Typography>
    </Box>
  );
};

export default Reports;